alphavantage:
  url: http://localhost:${wiremock.server.port}
spring:
  profiles:
    active: test
  session:
    store-type: redis
  jackson:
    default-property-inclusion: non_null
  data:
    redis:
      host: redis
      port: 6379
